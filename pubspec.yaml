name: firebase_auth_client
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_auth: ^4.1.1
  google_sign_in: ^5.2.1
  flutter_facebook_auth: ^3.5.6
  mocktail: ^0.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  google_sign_in_mocks: ^0.2.1

flutter:
